# 한끼잇다(temp-react) 백엔드 연동 안내서

이 문서 하나만 읽으면 이 프로젝트가 무엇인지, 어떻게 실행해보는지, 어떤 API를 만들어서 어떻게 연결해야 하는지 전부 알 수 있도록 정리했습니다. 이 대화의 이전 맥락을 모르는 분이 `temp-react` 폴더만 통째로 받아서 보는 상황을 가정하고 썼습니다.

---

## 0. 프로젝트가 뭔가요

"한끼잇다"는 매장/기관에서 남는 도시락(잉여 식품)을 필요한 사람에게 나눠주는 서비스의 MVP입니다.

- 사용자는 지도에서 매장을 찾아 도시락을 **신청**할 수 있습니다.
- 매장/기관 담당자는 **소속 인증**을 받으면 잉여 식품을 **등록**할 수 있습니다.
- 지금 이 저장소는 **React 프론트엔드만 완성된 상태**이고, 백엔드 서버는 아직 없습니다. 화면과 상호작용 로직은 다 만들어져 있고, 실제 데이터를 주고받을 자리(API 호출)만 비어 있는 상태라고 보면 됩니다.

---

## 1. 로컬에서 실행해보기

```bash
cd temp-react
npm install
cp .env.example .env   # 그리고 .env 안의 값을 채워주세요 (2번 항목 참고)
npm start
```

브라우저에서 `http://localhost:3000`이 열립니다. **백엔드가 없어도** 로그인 버튼부터 모든 화면 이동까지는 정상적으로 동작합니다. 다만 목록/프로필처럼 서버 데이터가 필요한 부분은 빈 화면이거나 "서버 응답을 해석할 수 없습니다..." 같은 에러 문구가 보이는데, 이는 `REACT_APP_API_BASE_URL`이 실제 서버를 가리키지 않아서 생기는 정상적인 현상입니다.

### 참고: 개발 환경
- Node.js, npm이 설치되어 있어야 합니다 (이 프로젝트는 Node 20+ 에서 확인했습니다).
- 프레임워크는 `react-scripts`(Create React App) 5.0.1, React 19.

---

## 2. 환경 변수 (.env)

`.env.example`을 복사해서 `.env` 파일을 만들고 아래 두 값을 채워주세요.

```
REACT_APP_API_BASE_URL=http://localhost:8080
REACT_APP_KAKAO_MAP_KEY=발급받은_카카오맵_JavaScript_키
REACT_APP_KAKAO_CLIENT_ID=카카오_로그인용_REST_API_키
REACT_APP_KAKAO_REDIRECT_URI=http://localhost:3000
```

- `REACT_APP_API_BASE_URL` — 백엔드 서버 주소입니다(끝에 `/` 없이). 프론트의 모든 API 요청이 이 주소 뒤에 경로를 붙여서 나갑니다. (예: `http://localhost:8080/api/stores`)
- `REACT_APP_KAKAO_MAP_KEY` — 홈 화면 지도를 띄우기 위한 카카오맵 키입니다. 발급 방법은 8번 항목 참고. 이 값이 없으면 지도 자리에 안내 문구만 뜨고 나머지 기능은 정상 동작합니다.
- `REACT_APP_KAKAO_CLIENT_ID` / `REACT_APP_KAKAO_REDIRECT_URI` — 카카오 로그인용 REST API 키와, 카카오 개발자 콘솔에 등록해둔 리다이렉트 URI입니다. 자세한 내용은 5번 항목 참고. 둘 다 비어있으면 로그인 버튼은 (개발 편의상) 그냥 홈 화면으로 이동합니다.

`.env`는 `.gitignore`에 이미 추가되어 있어서 커밋되지 않습니다.

---

## 3. 폴더 구조

```
src/
  api/                  ← 백엔드를 호출하는 함수들 (여기가 핵심!)
    client.js            공용 fetch 래퍼: 인증 토큰 자동 첨부, 에러 처리
    auth.js               카카오 로그인
    stores.js              매장/도시락 조회
    applications.js        도시락 신청/내역
    registrations.js       잉여 식품 등록/내역
    profile.js              내 프로필/통계/회원탈퇴
    favorites.js             찜한 매장
    notifications.js         알림 설정
    verification.js          소속 인증
    support.js                고객센터 문의
    adminClient.js            관리자 전용 fetch 래퍼 (일반 사용자와 토큰 분리)
    adminVerifications.js     관리자 로그인 + 소속 인증 승인/반려
  lib/
    kakaoMap.js           카카오맵 SDK 동적 로더
    geo.js                두 좌표 사이 거리 계산 (거리기반 추천에 사용)
  App.js                  화면 컴포넌트 전부 + 최상위 상태 관리 (파일 하나에 다 있습니다)
  App.css                 스타일
.env.example              환경변수 예시
package.json
```

`App.js`가 크긴 하지만, 화면 하나당 함수 컴포넌트 하나로 나뉘어 있고 각 컴포넌트 위에 `/*──── 화면: OO ────*/` 주석으로 구간이 표시되어 있어서 검색해서 찾기 쉽습니다. 최상위 `App` 컴포넌트(파일 맨 아래, `export default function App()`)가 로그인 이후 필요한 데이터를 한 번에 불러와서 각 화면에 props로 내려주는 구조입니다.

---

## 4. 화면 흐름 (사용자 입장에서)

```
로그인 화면 (카카오 로그인 버튼)
  └─ (최초 로그인이면) 회원가입 화면 — 역할 선택(일반 사용자 / 제공자) + 이름/전화번호
       └─ 제공자 선택 시 소속 인증 화면으로 자동 이동
  └─ 홈 화면 (지도 ⇄ 목록 전환 + 검색창, 위치 허용 시 거리순 정렬)
       ├─ 매장 선택 → 도시락 신청 화면 (매장 정보 + 메뉴 + 수령시간 입력 + 신청)
       ├─ 헤더 메뉴 > 식품 등록 → (소속 인증 안 된 사용자는 안내 화면만 보임)
       └─ 헤더 메뉴 > 마이페이지
            ├─ 프로필 수정
            ├─ 소속 인증 (인증되면 "식품 등록" 잠금 해제)
            ├─ 신청 내역 (취소 — 수령 30분 전부터 불가 / 수락된 건은 수령 QR 코드 보기)
            ├─ 등록 내역 (정보 수정 / 신청자 관리 — 수락·거절, 수락 후 QR로 수령 확인)
            ├─ 찜한 매장 (지도/신청 화면에서 하트 버튼으로 추가, 여기서 삭제)
            ├─ 알림 설정 (토글 3개)
            ├─ 계정 관리 (회원탈퇴 포함)
            └─ 고객센터 (FAQ + 1:1 문의)
```

---

## 5. 인증(로그인) 연동

**실제 카카오 OAuth 리다이렉트 플로우가 이제 프론트에 연결되어 있습니다.**

1. "카카오 로그인" 버튼 클릭 → `REACT_APP_KAKAO_CLIENT_ID`가 설정되어 있으면 카카오 인가 URL로 리다이렉트합니다 (`https://kauth.kakao.com/oauth/authorize?client_id=...&redirect_uri=...&response_type=code`). **설정이 안 되어 있으면(개발 중 기본값) 그냥 바로 홈 화면으로 이동하는 임시 동작을 유지합니다.**
2. 사용자가 카카오에서 동의하면 `REACT_APP_KAKAO_REDIRECT_URI?code=xxx`로 우리 사이트에 돌아옵니다.
3. `App` 컴포넌트가 마운트 시 URL의 `code` 파라미터를 감지해서 자동으로 `loginWithKakao(code)`를 호출합니다 — 내부에서 `POST /api/auth/kakao`를 호출하고, 응답의 `token`을 `localStorage`(`hankki_auth_token`)에 저장합니다. 처리 후 `code` 파라미터는 URL에서 제거합니다.
4. 응답에 `isNewUser: true`가 오면 **회원가입 화면(역할 선택)**으로, 아니면 바로 홈 화면으로 이동합니다.
5. 이후 모든 API 요청에 `Authorization: Bearer <token>` 헤더가 자동으로 붙습니다.

백엔드가 만들어야 할 것:
- `POST /api/auth/kakao` — body `{ code }` → 카카오 서버와 토큰 교환 + 회원 조회/생성 후 `{ token, isNewUser }` 반환. `isNewUser`를 정확히 내려줘야 회원가입 화면으로 잘 분기됩니다.
- 카카오 개발자 콘솔의 "Redirect URI"에 `REACT_APP_KAKAO_REDIRECT_URI`와 동일한 주소를 등록해두어야 합니다.

### 회원가입 (최초 로그인 시 역할 선택)

`isNewUser`인 사용자는 회원가입 화면에서 **일반 사용자 / 제공자** 중 하나를 고르고 이름·전화번호를 입력합니다. 제공자를 고르면 가입 완료 후 자동으로 소속 인증 화면(6번 항목)으로 넘어갑니다.

- `POST /api/users/me/signup` — body `{ role: "user" | "provider", name, phone }` → 응답 `{ name, phone, role }`. 이 호출 이후 사용자가 실제로 "제공자"인지(=식품 등록 가능 여부의 전제)는 6번 항목의 소속 인증 절차로 별도 확인됩니다 — role 선택은 온보딩 UX일 뿐, 권한 자체는 여전히 `verification.verified`로 판단해주세요.

---

## 6. 소속 인증 & 심사(pending) 플로우

매장/기관 담당자가 "식품 등록" 기능을 쓰려면 마이페이지에서 소속 인증을 받아야 합니다. **즉시 승인**과 **관리자 심사** 두 가지 방식을 모두 지원하도록 프론트가 이미 구현되어 있으니, 백엔드 정책에 맞는 쪽으로 응답만 내려주면 됩니다.

`POST /api/verifications` 요청에 대해:

- **즉시 승인하는 서비스라면** → 응답에 `{ verified: true, org, status: "approved" }`를 내려주세요. 프론트는 바로 "인증 완료" 화면을 보여주고 식품 등록 잠금을 풉니다.
- **관리자 심사가 필요한 서비스라면** → 응답에 `{ verified: false, org, status: "pending" }`를 내려주세요. 프론트는 자동으로 "인증 심사 중이에요" 화면과 마이페이지의 "⏳ 심사중" 배지를 보여주고, 식품 등록은 계속 잠긴 상태로 유지합니다.
- 심사가 끝나 관리자가 승인/반려하면, 사용자가 앱을 다시 열거나 마이페이지를 재방문할 때 `GET /api/verifications/me`가 최신 상태(`{ verified, org, status }`, status는 `"none" | "pending" | "approved" | "rejected"`)를 반환하도록 해주세요. **실시간 알림(push)까지는 프론트에 구현되어 있지 않고, 재조회 시점에 반영되는 구조**입니다.
- `status: "rejected"`를 내려주면, 재신청 폼 위에 "이전 신청이 반려되었어요" 안내 문구가 자동으로 표시됩니다.
- 사용자가 인증을 해제하거나 심사 신청을 취소하면 `DELETE /api/verifications/me`가 호출됩니다. 이 요청 후에는 프론트가 무조건 `{ verified: false, status: "none" }`으로 되돌립니다.

---

## 7. API 명세 (전체 엔드포인트)

모든 응답은 JSON이며, 실패 시 `{ "message": "사람이 읽을 수 있는 에러 메시지" }` 형태를 기대합니다(프론트가 이 `message`를 그대로 alert 등으로 사용자에게 보여줍니다).

### 인증
| Method | Path | Body | Response |
|---|---|---|---|
| POST | `/api/auth/kakao` | `{ code }` | `{ token, isNewUser }` |

### 매장 / 도시락
| Method | Path | Query/Body | Response |
|---|---|---|---|
| GET | `/api/stores` | `?keyword=`(선택) | `[{ id, name, address, lat, lng, hours, phone, photoUrl }]` |
| GET | `/api/stores/:storeId` | - | `{ id, name, address, lat, lng, hours, phone, photoUrl }` |
| GET | `/api/stores/:storeId/lunchbox` | - | `{ registrationId, menuItems: string[], allergyItems: string[], photoUrl }` |
| GET | `/api/stores/mine` | - | `[{ id, name, address, lat, lng, hours, phone, photoUrl }]` (내가 관리하는 매장 목록 — 식품 등록 화면에서 사용) |

`lat`/`lng`는 지도 마커 좌표(위경도)입니다. 주소 → 좌표 변환(지오코딩)은 백엔드에서 매장 등록 시 카카오 로컬 API로 처리하거나, 매장 등록 폼에서 좌표를 직접 입력받는 방식 중 편한 쪽으로 하면 됩니다. 좌표가 없는 매장은 지도에 표시되지 않습니다.

`/api/stores/mine`은 **한 계정이 여러 매장을 관리할 수 있다는 전제**로 배열을 반환합니다. 계정과 매장을 몇 대 몇으로 연결할지(1:1, 1:N)는 백엔드에서 정하면 됩니다 — 예를 들어 소속 인증 시 매장을 하나씩 등록/승인받는 구조로 설계하면 자연스럽게 N개가 쌓입니다. 프론트는 이 목록이 1개면 자동으로 선택하고, 2개 이상이면 등록 화면 상단에 매장을 고르는 탭을 보여줍니다.

### 도시락 신청
| Method | Path | Body | Response |
|---|---|---|---|
| POST | `/api/applications` | `{ storeId, registrationId, pickupTime }` | `{ id, storeId, registrationId, storeName, pickupTime, status, createdAt }` |
| GET | `/api/applications/me` | - | `[{ id, store, date, time, status, pickupCode }]` — status: `"수령대기" \| "수락됨" \| "수령완료" \| "취소됨"` |
| POST | `/api/applications/:applicationId/cancel` | - | - |

- `registrationId`는 이 신청이 어떤 잉여 식품 등록건에 대한 것인지 나타냅니다(아래 "등록건별 신청자 관리" 참고). `GET /api/stores/:storeId/lunchbox` 응답의 `registrationId` 값을 그대로 신청 요청에 담아 보냅니다.
- `pickupCode`는 **status가 "수락됨"인 신청에만** 내려주세요. 신청 내역 화면에서 이 값을 QR 코드로 보여주고(5번 항목 참고), 현장에서 제공자가 스캔/입력해 수령을 확인합니다.
- ⚠️ **취소(`/cancel`)는 반드시 백엔드에서도 "수령 30분 전부터는 취소 불가" 규칙을 검사해주세요.** 프론트는 UI에서 버튼을 비활성화할 뿐이라, API를 직접 호출하면 우회할 수 있습니다.

### 잉여 식품 등록
| Method | Path | Body | Response |
|---|---|---|---|
| POST | `/api/food-registrations` | `multipart/form-data`: `storeId`, `menuItems[]`(반복), `allergyItems[]`(반복), `quantity`, `photo`(선택) | `{ id, storeId, menu, quantity, date, status }` |
| GET | `/api/food-registrations/me` | - | `[{ id, storeId, menu, quantity, date, status, applicantCount }]` — status 예: `"등록됨" \| "마감됨" \| "수령완료"`, `applicantCount`(선택)는 등록 내역 목록에 "신청 N건"으로 표시됩니다 |
| GET | `/api/food-registrations/:registrationId` | - | `{ id, storeId, menuItems, allergyItems, quantity, photoUrl }` (정보 수정 화면에서 기존 값 불러올 때 사용) |
| PATCH | `/api/food-registrations/:registrationId` | `{ menuItems, allergyItems, quantity }` | - (음식 정보 변경) |

⚠️ 식품 등록/수정은 **소속 인증이 완료된 사용자만** 호출할 수 있어야 합니다. 프론트는 UI에서만 막고 있으므로, 백엔드에서도 반드시 인증 여부를 검사해서 인증 안 된 사용자의 요청은 401/403으로 막아주세요. 여러 매장을 관리하는 계정이라면 요청에 담긴 `storeId`가 실제로 그 계정이 관리하는 매장인지도 검증해주세요.

### 등록건별 신청자 관리 (식품 등록자가 신청을 수락/거절, 현장 QR 수령 확인)
| Method | Path | Body | Response |
|---|---|---|---|
| GET | `/api/food-registrations/:registrationId/applicants` | - | `[{ id, applicantName, pickupTime, status }]` — `id`는 application id, status: `"대기중" \| "수락됨" \| "거절됨" \| "수령완료"` |
| POST | `/api/food-registrations/:registrationId/applicants/:applicationId/respond` | `{ decision: "accept" \| "reject" }` | - |
| POST | `/api/food-registrations/:registrationId/applicants/:applicationId/verify-pickup` | `{ code }` | - |

마이페이지 > 등록 내역에서 등록건을 하나 누르면 "신청자 관리" 화면으로 들어가 이 API들을 사용합니다.
- 수락/거절 시 해당 신청자의 `GET /api/applications/me` 상태도 함께 갱신해주세요(신청자 쪽 "신청 내역" 화면에도 반영되어야 하므로).
- **"현장 QR 수령"은 카메라로 QR을 직접 스캔하는 방식이 아니라, 신청자 화면에 뜬 QR/코드 값(`pickupCode`)을 제공자가 입력해서 확인하는 방식으로 구현했습니다** (8-1번 항목에 이유 설명). `verify-pickup`에 보낸 `code`가 해당 신청의 `pickupCode`와 일치하면 그 신청의 status를 `"수령완료"`로 바꿔주세요.

### 내 프로필
| Method | Path | Body | Response |
|---|---|---|---|
| GET | `/api/users/me` | - | `{ name, email, phone, avatarUrl }` |
| PATCH | `/api/users/me` | `{ name, email, phone }` | `{ name, email, phone, avatarUrl }` |
| POST | `/api/users/me/avatar` | `multipart/form-data`: `avatar`(file) | `{ avatarUrl }` |
| GET | `/api/users/me/stats` | - | `{ applicationCount, registrationCount, activeDays }` (마이페이지 상단 3칸) |
| DELETE | `/api/users/me` | - | - (회원 탈퇴) |

### 찜한 매장
| Method | Path | Body | Response |
|---|---|---|---|
| GET | `/api/favorites` | - | `[{ id, name, address }]` (id는 매장 id) |
| POST | `/api/favorites` | `{ storeId }` | - |
| DELETE | `/api/favorites/:storeId` | - | - |

### 알림 설정
| Method | Path | Body | Response |
|---|---|---|---|
| GET | `/api/users/me/notification-settings` | - | `{ pickup, newListing, marketing }` (boolean) |
| PATCH | `/api/users/me/notification-settings` | `{ pickup, newListing, marketing }` | - |

### 소속 인증
| Method | Path | Body | Response |
|---|---|---|---|
| GET | `/api/verifications/me` | - | `{ verified, org, status }` |
| POST | `/api/verifications` | `multipart/form-data`: `org`, `document`(선택) | `{ verified, org, status }` (6번 항목 참고) |
| DELETE | `/api/verifications/me` | - | - |

### 고객센터
| Method | Path | Body | Response |
|---|---|---|---|
| POST | `/api/support/inquiries` | `{ message }` | - |

---

## 8. 카카오맵 연동 방법 (지도 + 검색)

지도는 이미 카카오맵 JavaScript SDK로 완전히 구현되어 있습니다(`src/lib/kakaoMap.js`, `App.js`의 `MapArea` 컴포넌트). 백엔드 개발자가 직접 할 일은 **키 발급과 "매장 좌표 데이터"를 잘 채워주는 것**뿐입니다.

1. https://developers.kakao.com → 로그인 → "내 애플리케이션" → 애플리케이션 추가
2. "앱 키" 메뉴에서 **JavaScript 키** 복사
3. "플랫폼 > Web 플랫폼 등록"에 서비스 도메인 등록 (로컬 테스트 시 `http://localhost:3000`도 등록)
4. "제품 설정 > 카카오맵" 활성화 확인
5. `.env`의 `REACT_APP_KAKAO_MAP_KEY`에 붙여넣고 `npm start` 재시작

**동작 방식**
- 지도는 기본적으로 부산시청 좌표(35.1796, 129.0756)를 중심으로 뜹니다.
- 홈 화면 진입 시 `GET /api/stores`로 전체 매장 목록을 한 번 불러와 지도에 마커로 표시합니다.
- 검색창에 입력한 검색어는 **이미 불러온 매장 목록을 이름/주소로 프론트에서 필터링**하는 방식입니다(별도 검색 API 호출 없음). 나중에 매장이 많아져 서버 검색이 필요해지면, `fetchStores({ keyword })`가 이미 `?keyword=` 쿼리를 보내도록 되어 있으니 `GET /api/stores?keyword=`만 지원하도록 백엔드를 확장하면 됩니다.
- 마커 클릭 → 해당 매장 정보를 들고 도시락 신청 화면으로 이동 → `GET /api/stores/:storeId/lunchbox` 호출.

### 8-1. 목록 조회 + 거리기반 추천

홈 화면에 지도/목록 전환 탭이 있습니다. "목록"을 누르면 카드 형태 리스트로 보이고, 브라우저 위치 권한을 허용하면 자동으로 **가까운 매장순**으로 정렬되고 각 카드에 거리("850m", "1.3km" 등)가 표시됩니다.

- 거리 계산은 `src/lib/geo.js`의 Haversine 공식으로 **프론트에서 직접** 계산합니다 — `GET /api/stores`가 내려주는 `lat`/`lng`와 브라우저의 `navigator.geolocation`으로 얻은 현재 좌표만 있으면 되므로, 백엔드에서 추가로 해야 할 일은 없습니다.
- 위치 권한을 거부해도 앱은 정상 동작합니다(그냥 정렬 없이 원래 순서로 표시).
- 매장이 아주 많아지면 서버 사이드에서 반경 검색(`?swLat=&swLng=&neLat=&neLng=` 같은 bounds 쿼리)을 지원하도록 확장할 수 있습니다 — 지금은 전체 목록을 한 번에 받아온다는 전제입니다.

### 8-2. 현장 QR 수령 방식 안내

"QR로 수령 확인"은 **카메라로 QR을 스캔하는 방식이 아니라, 신청자 화면에 뜬 QR 코드를 제공자가 확인 후 코드 값을 입력하는 방식**으로 구현했습니다.

- 신청자: 신청 내역에서 수락된 건을 열면 `qrcode.react` 라이브러리로 만든 QR 코드가 뜹니다(값은 백엔드가 내려준 `pickupCode`).
- 제공자: 신청자 관리 화면에서 수락된 신청에 "📷 QR 수령 확인" 버튼이 있고, 누르면 코드 입력창이 뜹니다. 여기에 고객이 보여준 QR의 값을 **직접 입력하거나, 매장에 있는 (키보드 입력처럼 동작하는) USB 바코드/QR 스캐너로 스캔**하면 자동으로 입력됩니다 — 이 방식은 실제 매장에서 흔히 쓰는 저비용 구성입니다.
- 브라우저 카메라로 직접 QR을 읽는 기능(예: `html5-qrcode` 라이브러리 연동)은 지금 범위에는 없습니다. 필요하면 이 입력창을 카메라 스캔 컴포넌트로 교체하는 방식으로 나중에 확장할 수 있습니다 — API(`verify-pickup`)는 동일하게 재사용됩니다.

---

## 9. 코드에서 실제로 API를 호출하는 위치 (구조 파악용)

| 화면(컴포넌트) | 호출하는 함수 | 시점 |
|---|---|---|
| `App` (최상위) | `loginWithKakao` | 카카오 로그인 콜백(`?code=`) 감지 시 자동 호출 |
| `SignupScreen` | `completeSignup` | "시작하기" 클릭 (App의 `handleCompleteSignup`) |
| `App` (최상위) | `fetchMyProfile`, `fetchMyVerification`, `fetchMyStats`, `fetchFavorites`, `fetchNotificationSettings` | 로그인 직후 1회 (`bootstrappedRef`) |
| `App` (최상위) | `fetchStores` | 로그인 후, 매장 목록이 비어있을 때 1회 |
| `HomeScreen` / `MapArea` / `StoreListView` | (props로 받은 stores 사용, 거리는 `lib/geo.js`로 클라이언트 계산) | 지도 마커 / 목록 카드 렌더링 |
| `ApplicationScreen` | `fetchLunchbox` | 화면 진입 시(선택된 매장 기준) |
| `ApplicationScreen` | `applyLunchbox` | "신청 하기" 클릭 |
| `RegisterScreen` | `fetchMyStores` | 화면 진입 시(소속 인증된 사용자) — 2개 이상이면 매장 선택 탭 표시 |
| `RegisterScreen` | `registerFood` | "등록 하기" 클릭 |
| `ApplicationsHistoryScreen` | `fetchMyApplications` | 화면 진입 시 |
| `ApplicationsHistoryScreen` | `cancelApplication` | "신청 취소" 클릭 (수령 30분 전이면 버튼 비활성화) |
| `ApplicationQRScreen` | (props로 받은 `pickupCode`를 QR로 렌더링) | 신청 내역에서 "QR 코드 보기" 클릭 시 진입 |
| `RegistrationsHistoryScreen` | `fetchMyRegistrations` | 화면 진입 시 |
| `EditRegistrationScreen` | `fetchRegistrationDetail`, `updateRegistration` | 화면 진입 시 불러오기 / "저장하기" 클릭 (등록 내역의 "정보 수정") |
| `RegistrationApplicantsScreen` | `fetchRegistrationApplicants` | 등록 내역에서 항목 클릭 시 (App의 `handleSelectRegistration`) |
| `RegistrationApplicantsScreen` | `respondToApplicant` | "수락"/"거절" 클릭 |
| `RegistrationApplicantsScreen` | `verifyPickup` | "📷 QR 수령 확인" 클릭 (수락된 신청에만 표시) |
| `FavoritesScreen` / `StoreCard`의 하트 버튼 | `addFavorite`, `removeFavorite` | 클릭 시 (App의 `handleToggleFavorite`/`handleRemoveFavorite`) |
| `NotificationSettingsScreen` | `updateNotificationSettings` | 토글 클릭 시 (App의 `handleToggleNotif`) |
| `ProfileEditScreen` | `updateMyProfile`, `uploadAvatar` | "저장하기" 클릭 (App의 `handleSaveProfile`) |
| `VerifyScreen` | `submitVerification`, `cancelVerification` | "인증 신청하기" / "인증 해제·취소" (App의 `handleVerify`/`handleUnverify`) |
| `AccountScreen` | `deleteAccount` | "회원 탈퇴" 클릭 (App의 `handleDeleteAccount`) |
| `SupportScreen` | `submitInquiry` | "1:1 문의하기" 클릭 |
| `AdminLoginScreen` | `adminLogin` | 관리자 로그인 폼 제출 |
| `AdminVerificationsScreen` | `fetchPendingVerifications`, `respondToVerification` | 화면 진입 시 / "승인"·"반려" 클릭 |

찜하기/알림 설정 토글은 **낙관적 업데이트**(먼저 화면을 바꾸고, 실패하면 되돌리는 방식)로 되어 있습니다. 백엔드가 실패 시 정확한 4xx/5xx 상태코드만 내려주면 프론트가 알아서 롤백합니다.

---

## 10. 연동 체크리스트 (이 순서로 진행하면 됩니다)

1. `.env`에 `REACT_APP_API_BASE_URL`을 로컬 백엔드 주소로 설정
2. `GET /api/stores`부터 구현 → 홈 화면 지도/목록에 매장이 뜨는지 확인 (카카오 키도 같이 설정해야 지도 자체가 보입니다)
3. `POST /api/auth/kakao` 구현 + 카카오 개발자 콘솔에 리다이렉트 URI 등록, `.env`에 `REACT_APP_KAKAO_CLIENT_ID`/`REACT_APP_KAKAO_REDIRECT_URI` 설정 (5번 항목) — `isNewUser`도 정확히 내려줘야 회원가입 화면 분기가 됩니다
4. `POST /api/users/me/signup` → 최초 로그인 시 회원가입(역할 선택) 확인
5. `GET/PATCH /api/users/me`, `GET /api/users/me/stats` → 마이페이지 프로필/통계 확인
6. `GET/POST/DELETE /api/verifications*` → 소속 인증 플로우 확인 (즉시 승인부터 먼저 테스트해보고, 필요하면 심사 플로우로 전환)
7. `GET /api/stores/:id/lunchbox`, `POST /api/applications`, `GET /api/applications/me` → 도시락 신청 플로우 확인
8. `POST /api/applications/:id/cancel` → 신청 취소 확인 (30분 제한은 백엔드에서도 검증)
9. `GET /api/stores/mine`, `POST /api/food-registrations`, `GET/PATCH /api/food-registrations/:id`, `GET /api/food-registrations/me` → 식품 등록/수정 플로우 확인 (소속 인증된 계정으로 테스트, 매장 여러 개 연결해서 선택 탭도 확인)
10. `GET /api/food-registrations/:id/applicants`, `POST .../respond`, `POST .../verify-pickup` → 신청자 수락/거절 + QR 수령 확인
11. `GET/POST/DELETE /api/favorites`, `GET/PATCH /api/users/me/notification-settings`, `POST /api/support/inquiries` → 나머지 마이페이지 기능
12. `POST /api/admin/login`, `GET /api/admin/verifications`, `POST /api/admin/verifications/:id/respond` → `/admin` 경로에서 관리자 로그인 후 인증 승인/반려 확인 (13번 항목)

각 단계마다 프론트 코드 수정 없이, 백엔드가 위 명세대로 응답을 내려주기만 하면 화면에 바로 반영됩니다.

---

## 11. 알려진 제약 / 다음 단계

- 신청/등록 내역, 매장 목록에 페이지네이션이 없습니다 — 지금은 전체 목록을 한 번에 받아온다고 가정합니다. 데이터가 많아지면 추가 필요.
- 매장/프로필 사진은 각각 별도 업로드 API로 분리되어 있습니다 — 백엔드 스토리지 정책(S3 등)에 맞춰 구현하면 됩니다.
- 한 계정이 여러 매장을 관리하는 구조는 지원하지만(`GET /api/stores/mine`), **매장을 새로 추가/등록하는 화면은 아직 없습니다**. 지금은 "이미 계정에 연결된 매장 목록 중 골라서 등록"만 가능하고, 계정↔매장 연결 자체는 백엔드(또는 관리자)가 처리한다고 가정합니다.
- 소속 인증 승인은 이 프론트엔드 안에 관리자 화면이 있습니다(13번 항목) — 다만 비밀번호 하나로 로그인하는 가장 단순한 방식입니다.
- 현장 QR 수령은 카메라 스캔이 아니라 코드 입력 방식입니다(8-2번 항목) — 카메라 스캔이 꼭 필요하면 추가 라이브러리 연동이 필요합니다.
- 제공자용 "현황 대시보드"는 등록 내역 + 신청자 관리 화면으로 기본기능은 되어 있지만, 매출/수령률 같은 통계 요약 화면은 아직 없습니다.

---

## 12. 백엔드 없이 프론트만 먼저 확인하고 싶다면

`.env`를 설정하지 않고 `npm start`만 실행해도 앱은 정상적으로 뜨고 모든 화면 이동이 가능합니다. 목록/프로필은 빈 상태 또는 에러 문구로 보이고, 지도는 안내 문구로 보이는 게 정상입니다(콘솔에 실패 로그만 찍히고 화면은 깨지지 않습니다). `REACT_APP_API_BASE_URL`을 실제 서버 주소로 바꾸는 순간, 코드 수정 없이 화면들이 실 데이터로 채워집니다.

---

## 13. 소속 인증 승인 — 관리자 화면 (구현 완료)

소속 인증 신청을 검토·승인·반려할 수 있는 **간단한 관리자 화면을 이 프로젝트 안에 이미 만들어뒀습니다.** 일반 사용자 화면(휴대폰 폭 고정 `.app-frame`)과는 분리된, 데스크톱에서 보기 편한 넓은 레이아웃입니다.

### 접속 방법
- 브라우저에서 **`/admin`** 경로로 접속하면 관리자 로그인 화면이 뜹니다. (배포 시 서버가 이 경로도 index.html로 서빙해주기만 하면 됩니다 — 대부분의 정적 호스팅/SPA 배포 설정에 이미 포함되어 있습니다.)
- 또는 일반 로그인 화면 하단의 "관리자이신가요?" 링크로도 들어갈 수 있습니다.
- 로그인에 성공하면 관리자 토큰을 일반 사용자 토큰과는 **별도의 localStorage 키**(`hankki_admin_token`)에 저장하므로, 관리자로 로그인한 상태와 일반 사용자로 로그인한 상태가 서로 섞이지 않습니다.

### 화면 구성
- **관리자 로그인** (`AdminLoginScreen`) — 비밀번호 하나만 입력받는 단순한 로그인 폼입니다.
- **소속 인증 심사** (`AdminVerificationsScreen`) — 대기 중인 인증 신청을 카드 목록으로 보여주고, 각 카드에 신청자 이름/소속명/제출일/첨부 서류 링크(있는 경우)와 "승인"/"반려" 버튼이 있습니다. 승인/반려하면 해당 카드가 목록에서 바로 사라집니다.

### 백엔드가 구현해야 할 API
| Method | Path | Body | Response |
|---|---|---|---|
| POST | `/api/admin/login` | `{ password }` | `{ token }` |
| GET | `/api/admin/verifications?status=pending` | - | `[{ id, name, org, documentUrl, submittedAt }]` — `documentUrl`은 서류 파일 다운로드/미리보기 링크(선택) |
| POST | `/api/admin/verifications/:verificationId/respond` | `{ decision: "approve" \| "reject" }` | - |

### [MVP 임시 조치] 백엔드 로그인 API 없이 먼저 써보기
`POST /api/admin/login`이 아직 없어도 관리자 화면 접속 자체는 바로 테스트할 수 있게 해뒀습니다. `.env`에 `REACT_APP_ADMIN_PASSWORD`를 설정해두면, 프론트가 서버를 호출하지 않고 **그 값과 직접 비교해서** 로그인시켜줍니다(`src/api/adminVerifications.js`의 `adminLogin`).

```
REACT_APP_ADMIN_PASSWORD=원하는_임시비밀번호
```

- 로그인만 이 방식으로 통과하고, 그 이후 목록 조회(`GET /api/admin/verifications`)·승인/반려는 여전히 실제 백엔드 API를 호출합니다. 백엔드가 없으면 그 부분은 에러 문구로 보이는 게 정상입니다.
- **`POST /api/admin/login`을 실제로 구현하면 `.env`에서 이 줄을 지우세요.** 그러면 코드 수정 없이 자동으로 서버 로그인 방식으로 전환됩니다.
- 이 값은 정말 임시용입니다 — 프론트 번들에 그대로 노출되는 값이라 공모전 심사/데모 이후에는 반드시 지우고 실제 백엔드 로그인으로 넘어가야 합니다.

`respond` 처리 시, 해당 신청과 연결된 사용자의 `GET /api/verifications/me` 결과가 승인이면 `{ verified: true, status: "approved" }`, 반려면 `{ verified: false, status: "rejected" }`로 바뀌도록 백엔드에서 함께 갱신해주세요 — 그래야 사용자 쪽 마이페이지에도 반영됩니다.

### 보안 관련 참고
- 지금은 **비밀번호 하나로 로그인하는 가장 단순한 방식**입니다. 실제 운영 전에는 관리자 계정을 여러 명이 나눠 쓸 경우 계정별 아이디/비밀번호로, 그리고 `POST /api/admin/login`은 반드시 HTTPS로만 호출되도록 해주세요.
- 관리자 API(`/api/admin/*`)는 프론트가 보내는 토큰이 실제로 유효한 관리자 토큰인지 백엔드에서 반드시 검증해야 합니다 — 프론트의 라우트 분리는 UX 편의일 뿐, 인증/인가는 전적으로 백엔드 책임입니다.
- 서류 파일은 아무나 접근 못 하도록(예: 서명된 임시 URL) 제공하는 것을 권장합니다.
